# Core
streamlit>=1.26.0,<2.0

# Model runtime (ONNX inference)
onnxruntime==1.15.1

# Replace opencv with headless variant for servers
opencv-python-headless==4.8.0.74

# Imaging + numerical libs
Pillow==9.5.0
numpy==1.25.2
requests==2.31.0

# Database
pymongo[srv]==4.5.1
gridfs==3.0

# (Optional) If you must use ultralytics, enable it explicitly below.
# NOTE: ultralytics pulls heavy deps including torch. If you get errors
# installing ultralytics on Streamlit Cloud, remove/comment it and use
# onnxruntime-based inference instead.
ultralytics==8.3.229
